package main

import "fmt"

func main() {
	s := "the sky is blue"
	fmt.Println("res", reverseWords(s))
}

func reverseWords(s string) string {
	for i, v := range s {
		if string(v) == ` ` {

		}
	}
	return ""
}
